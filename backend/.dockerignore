node_modules
dist
npm-debug.log
.env
.env.*
prisma/*.db
prisma/*.db-journal
# Module Prisma units live under prisma/modules/<name>/; the patterns above only match
# files directly under prisma/, so a scratch DB created there would be baked into the image.
prisma/**/*.db
prisma/**/*.db-journal
prisma/test-*.db*
Dockerfile
.dockerignore
